server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TonyGregg/config-repo.git
          searchPaths: licensingservice
#          basedir: /Users/genil/src/microservices/cloud/config-repo/

#Disable security of the management endpoint
management:
  security:
    enabled: false
